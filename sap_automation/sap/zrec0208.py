"""ZREC0208 — 공사발주서 리스트 (발주서 생성 및 TSRM 전송).

처리 개요(설계 문서 기준):
    1. 조회조건(회사코드 J000, 발주구분 20, 발주년도, 공사번호, 조건=공사발주서 생성) F8
    2. 해당 행 클릭 → Ctrl+Shift+F9(공사발주서생성 이동)
    3. 입력: 공사기간(형식 2026.01.01), 공사개요("PE ?m 인입공급관"),
       특기사항 1~4(1번은 ZMEC0210에서 읽은 산업안전보건관리비, 2~4는 고정 문구)
    4. Shift+F1 저장 → "예" → "저장되었습니다" 확인
    5. "TSRM 전송" → "예" → 전송 확인
       * 테스트 서버 모드일 때 TSRM 전송 단계 건너뜀

주의: 실제 자동화 로직은 이후 단계에서 구현한다. 지금은 뼈대(stub)만 둔다.
"""


def run(session, context):
    """ZREC0208 자동화를 실행한다(예정)."""
    raise NotImplementedError("ZREC0208 자동화 로직은 이후 단계에서 구현합니다.")

"""ZMEC0210 — 설계예산서 작성.

처리 개요(설계 문서 기준):
    1. 공사번호 입력 → "설계 LOCK" 해제
    2. [공사개요 탭] 압력/재질/관경 고정값, 시유지·사유지 연장값 입력
    3. [기타공사비 탭] 부위·자재 코드 입력(단가 매핑 로직: 투자비 ↔ 단가표 대조,
       불일치 시 PLP옵션 합산 재대조, 그래도 불일치 시 수동 처리 알림)
    4. Ctrl+S 저장 → "예"
    5. 공사비총괄표의 "산업안전보건관리비(배관)" 금액을 읽어 context에 저장
       → ZREC0208 특기사항 1번에 사용

주의: 실제 자동화 로직은 이후 단계에서 구현한다. 지금은 뼈대(stub)만 둔다.
"""


def run(session, context):
    """ZMEC0210 자동화를 실행한다(예정)."""
    raise NotImplementedError("ZMEC0210 자동화 로직은 이후 단계에서 구현합니다.")

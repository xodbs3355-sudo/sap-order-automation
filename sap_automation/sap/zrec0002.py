"""ZREC0002 — 공사의뢰 접수/반려.

처리 개요(설계 문서 기준):
    1. 조회 조건(회사코드 J000, 의뢰일자 1년 범위, "의뢰" 라디오) 설정 후 F8 실행
    2. 리스트 수집(의뢰일자, 의뢰자명, CMP No, 구간명, 연장, 투자승인비, 승인일자)
    3. 처리할 행 선택 → Ctrl+F11(접수)
    4. "접수처리를 하시겠습니까?" 팝업 → "예"

주의: 실제 자동화 로직은 이후 단계에서 구현한다. 지금은 뼈대(stub)만 둔다.
"""


def run(session, context):
    """ZREC0002 자동화를 실행한다(예정)."""
    raise NotImplementedError("ZREC0002 자동화 로직은 이후 단계에서 구현합니다.")

"""ZREC0100 — 공사번호 생성.

처리 개요(설계 문서 기준):
    1. 조회조건(회사코드 J000, 예산항목 1) F8 실행
    2. 좌측 CMP No 행 클릭 → "1:1 =>" 버튼 → 팝업 입력
       (공사구분 A, 발주구분 20, 굴착허가신청대상, 허가청 등)
    3. 저장 팝업 처리("예" / "아니오")
    4. 생성된 공사번호(예: 2026A0038)를 읽어 context에 저장 → 이후 단계 활용

주의: 실제 자동화 로직은 이후 단계에서 구현한다. 지금은 뼈대(stub)만 둔다.
"""


def run(session, context):
    """ZREC0100 자동화를 실행하고 생성된 공사번호를 반환한다(예정)."""
    raise NotImplementedError("ZREC0100 자동화 로직은 이후 단계에서 구현합니다.")
